build:
  hugo_version: '0.162.0'
deploy:
  # Deployment target: github-pages, netlify, vercel, cloudflare, or none
  # host: 'netlify'
  host: 'github-pages'
template:
  id: "kva-it"
  name: "kVA IT"
